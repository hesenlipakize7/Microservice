spring:
  application:
    name: verification-service

  datasource:
    url: jdbc:postgresql://localhost:5432/verification_db
    username: postgres
    password: 2504

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  port: 8080
